# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`deuce-aureum` is a Python project (based on `.gitignore` configuration). No code has been added yet.

## Setup

No build system, dependencies, or tooling has been configured yet. As the project grows, add commands here for installing dependencies, running tests, and linting.
